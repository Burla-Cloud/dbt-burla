---
import DocsLayout from "../../layouts/DocsLayout.astro";
import { withBase } from "../../utils/url";
---

<DocsLayout
  title="Documentation"
  description="dbt-burla documentation."
  lede="Everything you need to run dbt Python models on a Burla cluster. Start with the quickstart, then dig into how it works."
  next={{ title: "Quickstart", href: withBase("/docs/quickstart/") }}
>
  <h2>Start here</h2>
  <ul>
    <li><a href={withBase("/docs/quickstart/")}><strong>Quickstart</strong></a> — get a working dbt pipeline where a Python model runs on Burla in under 5 minutes.</li>
    <li><a href={withBase("/docs/how-it-works/")}><strong>How it works</strong></a> — end-to-end architecture and data flow.</li>
  </ul>

  <h2>Guides</h2>
  <ul>
    <li><a href={withBase("/docs/configuration/")}><strong>Configuration reference</strong></a> — every <code>burla_*</code> knob at profile and model level.</li>
    <li><a href={withBase("/docs/troubleshooting/")}><strong>Troubleshooting</strong></a> — common errors and fixes.</li>
  </ul>

  <h2>Warehouses</h2>
  <ul>
    <li><a href={withBase("/warehouses/duckdb/")}><strong>DuckDB</strong></a> — for local dev, CI, and demos.</li>
    <li><a href={withBase("/warehouses/snowflake/")}><strong>Snowflake</strong></a> — production Python models on elastic compute.</li>
    <li><a href={withBase("/warehouses/bigquery/")}><strong>BigQuery</strong></a> — skip Dataproc startup entirely.</li>
  </ul>

  <h2>Examples</h2>
  <p>Four runnable reference projects — see the <a href={withBase("/examples/")}>examples index</a>.</p>
</DocsLayout>
